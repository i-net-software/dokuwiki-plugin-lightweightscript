# dokuwiki-plugin-leightweightscript
Adds an "Editor and better only" script part and removes a lot of unneeded scripts parts for non editors. It makes the JS for end users a lot smaller and faster to load.
